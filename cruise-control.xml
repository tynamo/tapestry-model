<cruisecontrol>
  <project name="trails" buildafterfailed="false">
    <listeners>
      <currentbuildstatuslistener
          file="logs/trails/buildstatus.txt"/>
    </listeners>

    <!-- Bootstrappers are run every time the build runs,
        *before* the modification checks -->
    <bootstrappers>
    </bootstrappers>

    <!-- Defines where cruise looks for changes, to decide
         whether to run the build -->
    <modificationset quietperiod="10">
      <cvs localworkingcopy="trails/"/>
    </modificationset>

    <!-- Configures the actual build loop, how often and which
         build file/target -->
    <schedule interval="60">
      <ant antscript="ant"
           buildfile="trails/main-build.xml"
           target="build"
           uselogger="true"
           usedebug="false"/>
    </schedule>

    <!-- directory to write build logs to -->
    <log logdir="logs/trails">
    	<merge dir="trails/junit-reports/"/>
	</log>

    <!-- Publishers are run *after* a build completes -->
    <publishers>
    </publishers>
  </project>
</cruisecontrol>